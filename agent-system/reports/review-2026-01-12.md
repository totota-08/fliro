# 自動巡回レビューレポート

**日時**: 2026-01-12 10:30
**レビュアー**: reviewer-agent
**対象**: `src/pages/`

---

## サマリー

| 項目              | 件数 |
| ----------------- | ---- |
| 総タスク数        | 16   |
| 新規検出          | 5    |
| 既存タスク        | 11   |
| 高優先度 (high)   | 6    |
| 中優先度 (medium) | 8    |
| 低優先度 (low)    | 2    |

---

## 今回新規検出した問題 (rev-012 〜 rev-016)

### rev-012: CreateProjectPage.vue

- **重要度**: high
- **種別**: ハードコード色
- **内容**: `homeColors` 配列に HEX 色 (`#4f7c82`, `#0b2e33` 等) が直接ハードコード。CSS 内で `linear-gradient`, `rgba` 形式の色もハードコード。
- **行**: 33-42, 224-230

### rev-013: ProjectScoresPage.vue

- **重要度**: medium
- **種別**: ハードコード色
- **内容**: `.score-good`, `.score-warning` 等のクラスで `#4f7c82`, `#f59e0b`, `rgba(79, 124, 130, 0.15)` がハードコード。
- **行**: 398, 442, 446, 465, 469

### rev-014: ProjectTaskDetailPage.vue

- **重要度**: medium
- **種別**: ハードコード色
- **内容**: `.back-link:hover` や `.msg-body` で `rgba(79, 124, 130, 0.1)`, `rgba(255, 255, 255, 0.7)` がハードコード。
- **行**: 616, 918

### rev-015: InviteAcceptPage.vue

- **重要度**: medium
- **種別**: ハードコード色
- **内容**: `.signup-shell` のグラデーション背景に `rgba(184, 227, 233, 0.35)`, `rgba(147, 177, 181, 0.35)` がハードコード。`.form-error` に `rgba(214, 69, 69, 0.35)` がハードコード。
- **行**: 843-848, 1091

### rev-016: ProjectInvitesPage.vue

- **重要度**: medium
- **種別**: ハードコード色
- **内容**: `.invite-status--accepted`, `.invite-status--rejected` で `rgba(22, 163, 74, 0.25)`, `rgba(214, 69, 69, 0.25)` がハードコード。
- **行**: 845-857

---

## 問題なしと判断したファイル

以下のファイルは CSS 変数を適切に使用しており、購読解除も `onBeforeUnmount` で正しく行われていました：

- `src/pages/account/MyPage.vue` - 問題なし
- `src/pages/account/AccountSettingsPage.vue` - 問題なし
- `src/pages/projects/ProjectDashboardPage.vue` - 問題なし
- `src/pages/projects/ProjectActivityLogPage.vue` - 問題なし
- `src/pages/projects/ProjectNotificationsPage.vue` - 問題なし
- `src/pages/tasks/MyTasksPage.vue` - 問題なし (CSS 変数を使用)

---

## 違反カテゴリ別集計

| カテゴリ     | 件数 | 説明                     |
| ------------ | ---- | ------------------------ |
| design_token | 15   | デザイントークン以外禁止 |
| architecture | 1    | 責務分離違反             |

---

## 推奨アクション

1. **高優先度タスクから着手**
   - rev-004, rev-005, rev-006, rev-009, rev-012 を優先的に対応

2. **デザイントークン整備**
   - `--ui-brand-600-alpha-10`, `--ui-surface-glass`, `--ui-warning` 等の新規トークンを `ui-tokens.css` に追加検討

3. **責務分離の徹底**
   - rev-009: `ProjectChatPage.vue` の Firestore 操作を `services/` に移動

4. **デモページは後回し**
   - rev-007, rev-008 はデモページのため低優先度

---

## 次回巡回予定

- `src/components/` の巡回
- `src/composables/` の巡回

---

_このレポートは自動生成されました。_
